{
  "name": "daily-checkin",
  "version": "0.1.0",
  "description": "自动签到服务（Node.js + Playwright）",
  "type": "module",
  "private": true,
  "scripts": {
    "auto": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/one-click-run.ps1",
    "setup": "npm install && npx playwright install chromium",
    "start": "node src/index.js",
    "fill-login": "node scripts/fill-login-only.js",
    "login-only": "node scripts/fill-login-only.js --click-login",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
