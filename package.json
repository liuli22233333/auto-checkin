{
  "name": "daily-checkin",
  "version": "0.1.0",
  "description": "自动签到服务（Node.js + Playwright）",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "fill-login": "node scripts/fill-login-only.js",
    "login-only": "node scripts/fill-login-only.js --click-login",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
